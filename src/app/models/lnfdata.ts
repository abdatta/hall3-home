export class LnFData {
  constructor(
    public what: string,
    public when: string,
    public where: string,
    public contact: string,
    public type: boolean,
    public des?: string,
    public link?: string
  ) { }
}
